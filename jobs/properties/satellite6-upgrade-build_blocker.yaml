- property:
    name: satellite6-upgrade-build_blocker
    properties:
      - build-blocker:
          blocking-jobs:
            - "^upgrade-to.*"
            - "^automation-preupgrade.*"
            - "^upgrade-phase.*"
            - "^automation-postupgrade.*"
            - "^automation-upgraded.*"
