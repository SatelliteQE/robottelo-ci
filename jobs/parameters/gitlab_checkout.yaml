- parameter:
    name: gitlab_checkout
    parameters:
      - string:
          name: gitlabSourceBranch
          default: "{default-branch}"
          description: 'Source repository branch to build'
