- scm:
    name: robotello-ci_github
    scm:
        - git:
            url: 'https://$US_GIT_HOSTNAME/$US_GIT_ORGANIZATION/robottelo-ci.git'
            branches:
                - master
            skip-tag: true
            wipe-workspace: true
            basedir: 'robotello-ci'
